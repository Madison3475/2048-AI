# 构建产物：node tools/deploy_build.js 可随时重建，无需入库
dist/

# 权重文件 268 MB，超过 GitHub 100 MB 单文件上限；
# 如需版本管理用 Git LFS 或云盘备份，不要直接提交
weights.bin

# Cloudflare 部署 zip
2048-cloudflare.zip

# Node 依赖（本项目零依赖，防御性排除）
node_modules/
